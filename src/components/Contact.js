import React from 'react'

const Contact = (props) => (
    <section id="contact">
        <div className="inner">
            <section>
                foo
            </section>
            <section className="split">
                bar
            </section>
        </div>
    </section>
)

export default Contact
